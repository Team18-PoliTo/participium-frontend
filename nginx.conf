server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    # Essential for React Router - all routes fall back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}

